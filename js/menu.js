$(document).ready(function () {

  .on('.menu > li > ul', function (e) {
    var current = $(e.currentTarget),
    target = $('.megamenu'),
    isOpen = current.hasClass('show');

    if() {
      isOpen || target.addClass('hovered')
    } else {
      target.removeClass('hovered');
    }
  })
});

function() {
  var 
}
